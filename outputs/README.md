# Submission outputs

Copy your final group deliverables here before hand-in or archiving:

- `COMP41840_group_report.pdf` — **this is what you submit** where a PDF is required.
- `COMP41840_group_report.docx` — optional editable source for last-minute text edits.

Large binaries under `outputs/*.pdf` and `outputs/*.docx` are listed in `.gitignore` so they are not pushed by mistake; this small README can still be committed so everyone knows the convention.

**Cohort sizes for the report (aligned split):** train **452**, validation **97**, test **98** — see `results/patient_split.json`.
